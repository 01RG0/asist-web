# MongoDB Atlas Connection
# IMPORTANT: Replace <root> with your actual MongoDB password
MONGODB_URI=mongodb+srv://root:<root>@assistweb.u6rpliy.mongodb.net/attendance_system?retryWrites=true&w=majority&appName=assistweb

# JWT Configuration
# Generate a strong secret: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=change-this-to-a-secure-random-secret-in-production
JWT_EXPIRES_IN=30d
JWT_REFRESH_EXPIRES_IN=90d

# Server Configuration
PORT=5000
NODE_ENV=development
FRONTEND_URL=http://localhost:5000

# Note: SSL certificates not needed - Vercel provides HTTPS automatically
